WITH RAW_REGIONS
AS
(
 SELECT distinct 
 SLUG, 
 NAME, 
 PARENT
FROM {{ source ('dbtxeneta','regions')  }}
),
FINAL AS (
SELECT 
SLUG  AS REGION_ID, 
NAME AS REGION_NAME, 
PARENT AS PARENT_REGION
FROM RAW_REGIONS
)
SELECT * 
FROM FINAL