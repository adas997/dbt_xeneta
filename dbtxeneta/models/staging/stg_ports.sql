WITH RAW_PORTS
AS
(
 SELECT distinct 
 PID, 
 CODE, 
 SLUG, 
 NAME, 
 COUNTRY, 
 COUNTRY_CODE
FROM   {{ source ('dbtxeneta','ports')  }} 
),
FINAL AS (
SELECT 
PID  AS PORT_ID, 
CODE AS PORT_CODE, 
SLUG AS REGION_ID, 
NAME AS PORT_NAME, 
COUNTRY, 
COUNTRY_CODE
FROM RAW_PORTS
)
SELECT * 
FROM FINAL